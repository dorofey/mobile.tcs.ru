<?php foreach($this->catalog as $cat): ?>
    <div class="news-item">
        <a href="<?= $this->url(array('id'=>$cat->cat_id), 'catalogCatalog'); ?>/">
            <?php print $cat->cat_name; ?>
        </a>
        <div class="raq">&raquo;</div>
    </div>
<?php endforeach; ?>